describe('pipe', () => {
  it('creates record using remote service, with prepare function', () => {

  })

  it('updates record using remote service, with prepare function', () => {
  })

  it('checks for update and sends records to the local service, with cleanse function', () => {
  })
})
